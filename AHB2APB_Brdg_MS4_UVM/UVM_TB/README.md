# UVM Testbench

Milestone 4



**Using Makefile**
```bash
# Compile and run
make all

# Compile only
make compile

# Run simulation only (after compilation)
make sim

# Generate coverage report
make cov

# Clean generated files
make clean
```